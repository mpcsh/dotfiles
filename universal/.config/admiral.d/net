#!/usr/bin/env bash

source screen_vars
networkIcon=""

if ping -c 1 google.com >> /dev/null 2>&1; then
	NAME=$(iwgetid -r 2> /dev/null)
	if (( $? == 0 )); then
		echo "${networkIcon} $NAME"
	else
		echo "${networkIcon} Online"
	fi
else
	echo "%{B$FG}%{F$BG}${networkIcon} Offline%{B$BG}%{F$FG}"
fi	
