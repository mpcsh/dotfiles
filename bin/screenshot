#!/usr/bin/env bash

USAGE="Usage: screenshot [select]"

FILE=~/scrots/$(date +%F-%H%M%S).jpg

if [[ $1 == "select" && $# == 1 ]]; then
  MAIM_ARGS="-s"
elif [[ $# != 0 ]]; then
  echo $USAGE
  exit
fi

mkdir -p ~/scrots
maim $MAIM_ARGS -m 10 "$FILE"
~/dotfiles/bin/0x0 "$FILE"
notify-send "Screenshot uploaded"
