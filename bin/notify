#!/usr/bin/env bash

if [ -ne "/tmp/notify-$1.lck" ]; then
  polybar $1 &
  touch "/tmp/notify-$1.lck"
fi

echo -n $TIMEOUT_VAL > "/tmp/notify-$1.lck"

sleep 3

if [ $(pgrep -cx notify) -lt 2 ]; then
  kill "${_notify_${1}_pid}"
fi
